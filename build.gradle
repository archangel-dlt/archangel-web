

task buildFrontForDist(type: Copy, dependsOn: ':front-end:build') {
  from 'front-end/build'
  into 'back-end/src/ratpack/front-end'
}

task buildDist(dependsOn: [buildFrontForDist, ':back-end:distTar']) { }